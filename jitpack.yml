# This is only until I put this project on a proper repo manager.
# I shall do that as soon as I deem this project has enough features and is not buggy.
before_install:
  - sdk install java 17.0.1-tem
  - sdk use java 17.0.1-tem
install:
  - find ./ -name "*.lock" -type f -delete
  - ./gradlew clean install